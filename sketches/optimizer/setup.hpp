#pragma once

int resistors[] = {2, 5, 4, 3}; // resistor pins
int motors[] = {9, 10};         // motor pins
int pos[] = {60, 60};           // initial position of motors


int rest_time = 10000;          // time to wait when the light source is optimized
int sleep_time = 60000;         // time to wait when no light is present
