#pragma once

int resistors[] = {2, 5, 4, 3}; // resistor pins
int motors[] = {9, 10};         // motor pins
int pos[] = {0, 0};             // initial position of motors
long int forever = 1e10;        // when finished delay forever
